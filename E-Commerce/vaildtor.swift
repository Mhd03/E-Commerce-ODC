//
//  vaildtor.swift
//  E-Commerce
//
//  Created by Mohammad Abd El-Fattah Mohammd on 30/07/2023.
//

import Foundation
class Vaildator {
    /*
    static func isVaildEmail(for email: String) -> Bool {
        let email = email.trimmingCharacters(in: .whitespacesAndNewlines)
        let emailRegEx = "[A-Z0-9a-z._%+-]+@[A-Za-z0-9.-]+\\.{1}[A-Za-z]{2,64}"
        let emailProd = NSPredicate(format: "SElF MATCHES %@", emailRegEx)
        return emailProd.evaluate(with: email)
        
    }
    static func isVaildUsername(for username: String) -> Bool{
        let username = username.trimmingCharacters(in: .whitespacesAndNewlines)
        let usernameRegeEx = "\\w{4,24}"
        let usernameProd = NSPredicate(format: "SElF MATCHES %@", usernameRegeEx)
        return usernameProd.evaluate(with: username)
        
    }
    */
    /*
    static func ispasswordVaild(for password: String) -> Bool{
        let password = password.trimmingCharacters(in: .whitespacesAndNewlines)
        let passwordRegeEx = "^(?=.*[a-zA-Z])"
        let passwordProd = NSPredicate(format: "SElF MATCHES %@", passwordRegeEx)
        return passwordProd.evaluate(with: password)
    }
    */

}
